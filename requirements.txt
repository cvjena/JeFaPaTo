# image processing libs
numpy~=1.20
cmake; platform_system != "Darwin" or platform.machine != "arm64"
boost; platform_system != "Darwin" or platform.machine != "arm64"
opencv-python-headless~=4.5
mediapipe; platform_system != "Darwin" or platform.machine != "arm64"
mediapipe-silicon; platform_system == "Darwin" and platform.machine == "arm64"
protobuf>=3.11,<4

# general scientific libs
scipy~=1.8
pandas~=1.4
openpyxl

# gui libs
qtpy~=2.0
qtawesome~=1.1
PyQt6
# PySide6~=6.2  # requires a higher ubuntu version
pyqtgraph~=0.12
pyqtconfig~=0.9

# logging and utils
pluggy~=1.0
tabulate~=0.8
requests~=2.27
structlog~=21.5
rich~=12.0
psutil~=5.8
